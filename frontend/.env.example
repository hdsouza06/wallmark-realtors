# Base URL of the backend API.
#
# Local development (uses the Vite dev proxy in vite.config.js):
#   VITE_API_URL=/api
#
# Production (Vercel) — point to your deployed Render backend, including /api:
#   VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=/api

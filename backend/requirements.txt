# Pinned for Python 3.12 (Render free tier). Every package below ships prebuilt
# manylinux wheels for CPython 3.12, so pip never needs Rust/cargo/maturin to
# build from source. Do NOT run on Python 3.13/3.14 — pydantic-core has no
# wheels there yet and would fall back to a Rust source build.
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Pydantic v2 — pydantic-core is the Rust extension; 2.18.2 has cp312 wheels.
pydantic==2.7.1
pydantic-core==2.18.2
pydantic-settings==2.3.1

# Auth / crypto — these ship abi3 wheels (no compiler needed on 3.12).
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

python-multipart==0.0.9
cloudinary==1.44.2
slowapi==0.1.9
email-validator==2.1.1
python-dotenv==1.0.1
fastapi-mail==1.4.1
httpx==0.27.0
